spring.application.name=paiementservice
server.port=9010

# Eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Spring Cloud Config
spring.cloud.config.enabled=true
spring.config.import=optional:configserver:http://localhost:8888

# ===================================
# Database Configuration
# ===================================
spring.datasource.url=jdbc:postgresql://localhost:5432/paiementservice_db
spring.datasource.username=postgres
spring.datasource.password=Zak20.
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
rabbitmq.queue.payment.completed=payment.completed.queue
rabbitmq.queue.invoice.overdue=invoice.overdue.queue

# Logging
logging.level.root=ERROR
logging.level.com.example.paiementservice=DEBUG
logging.level.org.springframework.boot.web.embedded.tomcat=INFO
